<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="330dp"
    android:layout_height="wrap_content"
    android:background="@drawable/shape_radius54"
    android:backgroundTint="@color/white"
    android:padding="24dp">

    <androidx.appcompat.widget.AppCompatButton
        android:id="@+id/change_image_button"
        android:layout_width="match_parent"
        android:layout_height="55dp"
        android:background="@drawable/ripple_button"
        android:text="@string/change_the_image"
        android:textAppearance="@style/Text.OnButton"
        app:layout_constraintTop_toTopOf="parent" />

    <androidx.appcompat.widget.AppCompatButton
        android:id="@+id/remove_image_button"
        android:layout_width="match_parent"
        android:layout_height="55dp"
        android:layout_marginTop="8dp"
        android:background="@drawable/ripple_button_outlined_error"
        android:text="@string/remove_the_image"
        android:textAppearance="@style/Text.OnButton"
        android:textColor="?attr/colorError"
        app:layout_constraintTop_toBottomOf="@id/change_image_button" />
</androidx.constraintlayout.widget.ConstraintLayout>